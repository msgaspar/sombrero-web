import styled from 'styled-components';

export const Container = styled.div`
  svg,
  img {
    margin-top: 2.5px;
  }

  tr {
    font-size: 14px;
  }

  button {
    border-style: none;
    background-color: inherit;
  }
`;
